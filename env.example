# SeaTalk Phase 2 does not require user-visible .env variables.
#
# Configure SeaTalk accounts under ~/.hermes/config.yaml instead. The account
# app_secret and signing_secret values live in config.yaml, so protect that file
# as secret-bearing configuration.
#
# platforms:
#   seatalk:
#     enabled: true
#     extra:
#       accounts:
#         default:
#           enabled: true
#           app_id: your_app_id
#           app_secret: your_app_secret
#           signing_secret: your_signing_secret
#           mode: webhook
#           webhook_host: 0.0.0.0
#           webhook_port: 8080
#           webhook_path: /callback
#           dm_policy: allowlist
#           allow_from:
#             - alice@example.com
#           group_policy: open
#           group_allow_from:
#             - raw_seatalk_group_id
#           group_sender_allow_from:
#             - alice@example.com
#           processing_indicator: typing
#       home_channel_account_id: default
#       home_channel: group/raw_seatalk_group_id
